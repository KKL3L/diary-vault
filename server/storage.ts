// This file is intentionally simple as we're using client-side storage (IndexedDB)
// for this application instead of server-side database

export const storage = {
  // This is just a placeholder - actual storage is handled in client-side IndexedDB
  // If we needed server-side persistence in the future, we could implement it here
};
